/* Header functionality has been merged into AuthenticatedLayout's sidebar + topbar.
   This file is kept as a stub so any accidental imports don't break the build. */
export default function Header() { return null }
